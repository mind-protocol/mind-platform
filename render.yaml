# Render Blueprint — Mind Platform (Next.js Frontend)
#
# The L4 Registry backend and FalkorDB have been moved to the mind-protocol
# repository. See: https://github.com/mind-protocol/mind-protocol/render.yaml
#
# This blueprint now only covers the Next.js frontend if deployed on Render.
# The frontend should set L4_REGISTRY_URL to point to the mind-protocol
# deployment of the L4 Registry service.
#
# If the frontend is deployed on Vercel, this file is not needed.

services: []
