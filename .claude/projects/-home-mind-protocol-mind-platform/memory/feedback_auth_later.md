---
name: Auth implementation deferred
description: Auth wallet-first implementation is deferred. Do not work on it until user explicitly asks.
type: project
---

Auth implementation is parked. The user will revisit it later.

**Why:** Higher priority items first (schema sync, L4 backend for registry, FalkorDB E2E).

**How to apply:** Don't propose auth work unless asked. Focus on the two active chantiers.
