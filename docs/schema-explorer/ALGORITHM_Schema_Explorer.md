# ALGORITHM — Schema Explorer

```text
On request to /[locale]/schema:
  1. Resolve locale segment from route.
  2. Render static metadata for title/description.
  3. Render node type cards from local constant list.
  4. Render explanatory section for link semantics.
```
