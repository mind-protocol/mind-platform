# SYNC — Schema Explorer

## Maturity

STATUS: CANONICAL

## Current State

Schema Explorer page is now implemented as a static public route with protocol-aligned descriptions for node types and link semantics.

## Completed

- Created full module doc chain (OBJECTIVES → HEALTH).
- Implemented `/[locale]/schema` page with metadata and protocol summary.

## TODO

- [ ] Add direct cross-link to `docs/schema/schema.yaml` once a docs UI route is available.
- [ ] Add lightweight visual examples for node/link records.
