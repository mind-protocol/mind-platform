# BEHAVIORS — Schema Explorer

1. When `/[locale]/schema` loads, user sees a protocol-oriented overview.
2. Page displays the canonical five node types and one link model.
3. Page renders without requiring backend connectivity.
